<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Company Information</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h1 class="mb-4">Edit Company Information</h1>


      <%= form_with(model: @company, url: update_avatar_company_path, local: true, method: :put, html: { multipart: true }) do |form| %>
        <div class="form-group">
          <%= form.label :avatar, "Upload Avatar" %>
          <%= form.file_field :avatar, class: "form-control-file" %>
        </div>

        <%= form.submit "Save", class: "btn btn-primary mt-3" %>
      <% end %>

      <div class="mt-4">
        <% if @company.avatar.attached? %>
          <% @company.avatar.each do |avatar| %>
            <div class="mb-4 text-center">
              <%= image_tag url_for(avatar), class: "avatar img-thumbnail" %>
              <%= form_tag remove_avatar_company_path(@company), method: :delete, data: { confirm: "Are you sure you want to remove the avatar?" } do %>
                <%= submit_tag "Remove Avatar", class: "btn btn-danger mt-2" %>
              <% end %>
            </div>
          <% end %>
        <% else %>
          <p class="text-center">No avatar uploaded yet.</p>
        <% end %>
      </div>




      <%= form_with(model: @company, url: company_path, local: true, method: :put) do |form| %>

        <style>
            .form-label strong {
                font-weight: bold;
            }
        </style>

        <div class="mb-3">
          <span class="form-label"><strong><%= form.label :company_code %></strong></span>
          <%= form.text_field :company_code, class: "form-control" %>
        </div>

        <div class="mb-3">
          <span class="form-label"><strong><%= form.label :company_name %></strong></span>
          <%= form.text_field :company_name, class: "form-control" %>
        </div>

        <div class="mb-3">
          <span class="form-label"><strong><%= form.label :address %></strong></span>
          <%= form.text_field :address, class: "form-control" %>
        </div>

        <div class="mb-3">
          <span class="form-label"><strong><%= form.label :email %></strong></span>
          <%= form.text_field :email, class: "form-control" %>
        </div>

        <div class="mb-3">
          <span class="form-label"><strong><%= form.label :phone %></strong></span>
          <%= form.text_field :phone, class: "form-control" %>
        </div>

        <div class="mb-3">
          <span class="form-label"><strong><%= form.label :website %></strong></span>
          <%= form.text_field :website, class: "form-control" %>
        </div>

        <div class="mb-3">
          <span class="form-label"><strong><%= form.label :status %></strong></span>
          <%= form.text_field :status, class: "form-control" %>
        </div>

        <div class="mb-3">
          <%= form.submit 'SAVE', class: "btn btn-primary me-2" %>
          <%= link_to 'Back', company_path(@company.id), class: 'btn btn-secondary' %>
        </div>


    <% end %>

    </div>
  </div>
</div>

</body>
</html>
<style>
    .avatar {
        width: 300px; /* Đặt chiều rộng của avatar */
        height: 300px; /* Đặt chiều cao của avatar */
    }
</style>